mkdir -p bin
cat > bin/docker-entrypoint <<EOL
#!/bin/bash
set -e

# Remove a potentially pre-existing server.pid
rm -f /rails/tmp/pids/server.pid

# Then exec the container's main process
exec "\$@"
EOL
chmod +x bin/docker-entrypoint